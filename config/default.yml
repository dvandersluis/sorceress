# required tools
dependencies:
  - brew
  - ruby:
      dependencies:
        manager:
          - rbenv
          - rvm
          - chruby
          - asdf

# Sorceress steps (always run regardless of any user steps)
prerequisites:
  - find_dependencies
  - install_features

# User steps
steps: []
